// SwifterX — shared build settings (committed).
// For local Stripe keys, copy Secrets.xcconfig.example → Secrets.xcconfig (gitignored).

// Override in gitignored Secrets.xcconfig (see Secrets.xcconfig.example). REPLACE_ME keeps Release safe until a real key is set.
STRIPE_PUBLISHABLE_KEY = pk_test_REPLACE_ME

#include? "Secrets.xcconfig"
